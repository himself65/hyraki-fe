import { Action } from 'redux'

const INITIAL_STATE = {}

// todo
export default function drawer (state = INITIAL_STATE, action: Action<any>) {
  switch (action.type) {
    default:
      return {}
  }
}
